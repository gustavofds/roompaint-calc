<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bulma@0.9.1/css/bulma.min.css"
    />
    <link rel="stylesheet" href="style.css" />
    <title>Roompaint Calc</title>
  </head>
  <body>
    <section class="section">
      <div class="container">
        <h2 class="title is-2">Calcule quantas latas de tinta irá precisar!</h2>
        <p class="subtitle">Informe as medidas de cada parede e quantas portas/janelas cada uma tem. Ao final, iremos dizer quantas latas de tinta de cada tamanho você precisará para pintar sua sala.</p>
        <div>
          <h3 class="title is-3">Dados das Paredes</h3>
          <form id="walls-form" action="">
            <% new Array(wallsQty).fill(undefined).forEach((wall, index) => { %>
              <%- include('wallInput', { index }); %>
            <% }) %>
            <div class="field">
              <div class="control">
                <button type="submit" id="send-button" class="button is-link">Calcular</button>
              </div>
            </div>
          </form>
        </div>
      </div>
    </section>
    <script src="/js/bundle.js"></script>
  </body>
</html>
